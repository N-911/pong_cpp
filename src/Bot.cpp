#include "Bot.h"
#include <iostream>

using std::cout;
using std::endl;

Bot::Bot() { };

/*
void Bot::moving(std::shared_ptr<Ball> ball) {
  if (ball->get_speed().x < 0) {
    return;
  }
  if (ball->get_center().x + BALL_SIZE / 2 < H / 2) {
    return;
  } else if (ball->get_center().y + PLAYER_H / 4 > center.y) {
    m_sampleRect->y += (m_delta + 3);
  } else {
    m_sampleRect->y -= (m_delta + 3);
  }
  set_center();
}
*/
